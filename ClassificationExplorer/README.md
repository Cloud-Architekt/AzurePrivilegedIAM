# EntraOps Classification Explorer

A clean, fast, **static web app** to explore, navigate and understand the
[EntraOps](https://github.com/cloud-architekt/entraops) Enterprise Access Model
classification of roles, role actions and permissions across the Microsoft cloud.

It reads the classification JSON in this repository directly and turns it into an
interactive, Microsoft **Fluent UI / Azure Portal** styled experience.

![EntraOps](assets/EntraOps.png)

## What you can do

- **Overview** – a holistic dashboard of the tiered administration model: totals,
  per-RBAC-system distribution, and the Enterprise Access Model footprint.
- **Enterprise Access Model** – what Control / Management / User Access mean, why
  the planes exist, and links to Microsoft documentation.
- **Roles** – browse every classified role across **Microsoft Entra ID**, **Azure**
  and **Intune**. Search, filter by access level, system and privileged status, and
  open any role to see *why* it is classified the way it is, with its role actions
  grouped by access level. Entra ID roles whose effective definition (Microsoft
  Graph) differs from the public Microsoft Learn permissions reference are tagged
  with a ⇄ *doc mismatch* badge; opening the role highlights every role action that
  is **not in docs** (present in Graph but undocumented) or **not in definition**
  (documented on Learn but not returned by Graph).
- **Role Actions** – the atomic unit of classification. Every distinct role action /
  resource operation and the plane it grants, with the roles that include it.
- **API Permissions** – classified AppRoles, delegated Scopes and first-party
  Microsoft API permissions, filterable by type, resource app and access level.
- **Scope-aware Tiering** – how the *same* action changes plane depending on
  **where** it is granted, driven by the EntraOps `*.Param.json` scope placeholders
  (privileged users / service principals / groups / devices, Azure Tier 0/1 resource
  scopes, Intune Tier 0/1 group IDs).
- **Role Overwrites** – roles classified independently of their listed actions
  (`Classification_RoleDefinitionOverwrites.json`), each with its justification and a
  reference to the applicable role actions of the service.
- **Attack Paths** – well-documented privilege-escalation and lateral-movement paths
  that run through classified Control Plane role actions, each with an escalation
  chain, the enabling roles / role actions and references to public write-ups. An
  interactive BloodHound-style graph (nodes = techniques, roles, role actions and the
  Control Plane target; directed edges = how access is chained) lets you drag nodes
  and click through to the underlying role or action. Roles and role actions that
  participate in a path are tagged with an ⚠ *attack path* badge throughout the
  explorer.
- **History** – browse the `git log` of the classification files: every commit that
  added, removed or re-tiered a role, role action or API permission, with the exact
  diff against the previous version. Search a role, role action or permission by name
  to see its own change timeline across the whole history. A ⏱ *N changes in last
  update* badge on the Roles, Role Actions and API Permissions toolbars — plus a small
  ⏱ *new* / *changed* tag on the affected rows — links straight into the relevant
  commit. Every role, role action and API permission detail also has its own
  **⏱ View change history** link that jumps straight to its cross-commit timeline —
  including role actions, which have no entry of their own in the source files and are
  found by looking inside their owning role's added/removed/changed record. Generated by
  `Update-EntraOpsClassificationExplorerData` (git-log walk, skip with `-SkipHistory`); requires
  a full git clone (not a downloaded zip) and degrades to an empty state when no history
  is embedded.
- **Role Comparison** – pick two or more roles (via the ⇄ checkboxes in the Roles
  table, or "Compare with another role" in a role's details) to compare their access
  level, flags and full role-action matrix side by side, with differences highlighted.
- **Review list** – star any role, role action (including its scope) or API permission
  to collect it in a shortlist (kept in `localStorage`), each with a deep link back to
  where it was starred. Export the list to CSV from the ★ *Review list* button in the
  app bar.
- **Collapsible navigation** – minimize the left navigation rail to an icon-only strip
  (the « handle on its edge) for more working space; the preference is remembered.

## Access levels (Enterprise Access Model)

| Tier | Plane                | Meaning                                                                                                          |
| ---- | -------------------- | ---------------------------------------------------------------------------------------------------------------- |
| 0    | **Control Plane**    | Controls the identity & security fabric (roles, credentials, policy, sync). Compromise = tenant/estate takeover. |
| 1    | **Management Plane** | Manages workloads, apps, devices, groups and resources.                                                          |
| 2    | **User Access**      | Read / self-service with limited blast radius.                                                                   |

## Running locally

The app ships with the classification data **embedded as a generated JavaScript
bundle** (`data/classification-data.js`), so it runs entirely client-side with **no
web server and no Python** — just open `index.html`:

```bash
open ClassificationExplorer/index.html      # macOS
# or double-click index.html in any file manager
```

There is no runtime fetch fallback — the app only ever reads the embedded bundle, so
re-run the generator (below) whenever the classification data changes.

## Updating / generating the data

The embedded bundle is produced by the shared, mode-aware PowerShell generator, which
reads the latest classification files **directly from the repository** (the raw JSON is
no longer copied into the app folder). Use the command for the deployment you are
refreshing:

```powershell
# AzurePrivilegedIAM standalone repository
. ./Scripts/Update-EntraOpsClassificationExplorerData.ps1
Update-EntraOpsClassificationExplorerData -Mode Standalone
```

```powershell
# EntraOps repository (the generator is exported by the EntraOps module)
Import-Module ./EntraOps -Force
Update-EntraOpsClassificationExplorerData -Mode EntraOps
```

This validates every source file (sanitizing the `*.Param.json` scope placeholders the
same way the app does) and regenerates everything the app consumes:

| Output                                             | Contents                                                      |
| -------------------------------------------------- | ------------------------------------------------------------- |
| `data-manifest.json`                               | Timestamp, size, SHA-256 hash and item count per source file. |
| `data/classification-data.js` (`window.EOCE_DATA`) | The embedded classification bundle.                           |
| `data/attack-paths.js`                             | Attack-path catalog, from `content/attack-paths/*.md`.        |
| `data/tier-map.js`                                 | Overview (Enterprise Access Model Map) Sankey dataset.        |
| `data/history-data.js` (`window.EOCE_HISTORY`)     | Change history (`git log`) for the History view.              |

Re-run it whenever the classification data changes. Useful switches: `-WhatIf`,
`-Verbose`, `-SkipEmbed`, `-SkipManifest`, `-SkipHistory`.

The change history needs the `git` command line and a full (non-shallow) clone — run
the script from the repository, not from a downloaded zip. When git is unavailable, or
you want a faster refresh while iterating on classification JSON, pass `-SkipHistory`;
the History view simply shows an empty state and nothing else in the app depends on it.

## Hosting on GitHub Pages

Because the data is embedded, you can publish **only** the `ClassificationExplorer`
folder as a fully self-contained static site, or publish the whole repository with
**GitHub Pages from the repository root** (branch `main`, folder `/ (root)`). The app
will then be available at
`https://<owner>.github.io/<repo>/ClassificationExplorer/`. Re-run
`Update-EntraOpsClassificationExplorerData` and commit the refreshed bundle before publishing.

## Data sources

| Area                                                                              | File(s)                                                                                                           |
| --------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------- |
| Entra ID directory roles                                                          | `Classification/Classification_EntraIdDirectoryRoles.json`                                                        |
| Entra ID directory roles (Microsoft Learn reference, for doc-mismatch comparison) | `Classification/Classification_EntraIdDirectoryRolesFromMsftDocs.json`                                            |
| Identity Governance (Entitlement Management) roles                                | `Classification/Classification_IdentityGovernance.json`                                                           |
| Azure resource roles                                                              | `Classification/Classification_AzureResources.json`                                                               |
| Intune device management roles                                                    | `Classification/Classification_DeviceManagementRoles.json`                                                        |
| Microsoft Defender XDR permissions                                                | `EntraOps_Classification/Classification_Defender.json`                                                            |
| API permissions / AppRoles / Scopes                                               | `Classification/Classification_ApiPermissions.json`, `Classification_AppRoles.json`, `Classification_Scopes.json` |
| Scope-aware definitions                                                           | `EntraOps_Classification/*.Param.json`                                                                            |
| Role overwrites                                                                   | `EntraOps_Classification/Classification_RoleDefinitionOverwrites.json`                                            |

These paths are relative to the **repository root**; the script reads them from there
and embeds the data into the app. The `ClassificationExplorer` folder itself no longer
keeps duplicate copies of the JSON.

## Tech

Plain HTML, CSS and JavaScript — **no build step, no dependencies**. Just static
files. The classification data is the single source of truth and is never modified by
the app.
